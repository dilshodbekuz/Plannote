package uz.apprica.plannote.ui.theme

import androidx.compose.ui.graphics.Color

// ── Backgrounds ──────────────────────────────────────────────────────────────
val DarkBackground  = Color(0xFF0D0F14)
val DarkSurface     = Color(0xFF161B22)
val DarkCard        = Color(0xFF1E2530)
val DarkCardAlt     = Color(0xFF232B38)

// ── Brand ─────────────────────────────────────────────────────────────────────
val PrimaryTeal     = Color(0xFF4ECDC4)
val PrimaryTealDim  = Color(0xFF2A9D96)
val AccentAmber     = Color(0xFFFFB347)
val AccentPink      = Color(0xFFFF6B9D)
val AccentPurple    = Color(0xFFB39DDB)

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary     = Color(0xFFE8EAF6)
val TextSecondary   = Color(0xFF9E9E9E)
val TextHint        = Color(0xFF616161)

// ── Semantic ──────────────────────────────────────────────────────────────────
val SuccessGreen    = Color(0xFF4CAF50)
val WarningOrange   = Color(0xFFFF9800)
val ErrorRed        = Color(0xFFF44336)
val DividerColor    = Color(0xFF2A3040)

// ── Priority ──────────────────────────────────────────────────────────────────
val PriorityLow     = Color(0xFF4CAF50)   // yashil
val PriorityMedium  = Color(0xFF29B6F6)   // ko'k (past=yashil, o'rta=ko'k, yuqori=qizil)
val PriorityHigh    = Color(0xFFFF4444)   // qizil (avval pushti edi)

// ── Note card palitasi ────────────────────────────────────────────────────────
val NoteCardColors = listOf(
    Color(0xFF1E2530),   // 0 – default
    Color(0xFF1A2A35),   // 1 – ko'k
    Color(0xFF2A1A35),   // 2 – binafsha
    Color(0xFF1A352A),   // 3 – yashil
    Color(0xFF352A1A),   // 4 – to'q sariq
    Color(0xFF351A2A)    // 5 – pushti
)
